PROGRAM What(INPUT, OUTPUT);
VAR
  Ch: CHAR;
BEGIN
  READ(ch);
  WRITELN('Введите символБкоторый хотите проверить');
  IF '0'<= Ch
  THEN
    IF Ch <= '9'
    THEN
      WRITELN('YES', ch, '<=9')
    ELSE
      WRITELN('NO', ch, '>=9')
  ELSE
    WRITELN('NO' , '0>=', Ch)
END.